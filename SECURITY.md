# Security Policy

Agent Authority is security-sensitive. Report vulnerabilities privately rather than publishing exploit details. Protect policy files, identity bindings, authority tokens, signing keys, approval channels, and audit storage.

The project provides security controls, not a guarantee that an operator's configuration is safe. Production deployments should pin dependencies, isolate execution, and review policies before rollout.