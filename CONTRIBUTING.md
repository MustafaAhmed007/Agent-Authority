# Contributing

Keep changes focused. Add regression tests for authorization changes. Run `ruff check .` and `pytest -q`. Document security-sensitive behavior. Preserve deterministic and explainable decision paths.