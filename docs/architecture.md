# Architecture

Agent → Authority Kernel → Policy + Context + Risk → Approval/Budget → Execution Gate → Tool/API/MCP → Verification → Proof/Audit → Learning.

The kernel is local-first and vendor-neutral. MCP, framework adapters, sandbox drivers, persistent storage, replay, and cloud fleet management are extension layers.